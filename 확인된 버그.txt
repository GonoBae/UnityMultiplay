22-09-17
==수정완료==
다른 플레이어가 나간 후에 방장은 밖으로 나가지지 않음 ( RPC 를 해제함 )
 -> 나가기 버튼을 누르면 Null Ref 가 발생

방장이 나가면 모두가 나가짐 ( Sync Scene = false / SceneManager.LoadScene(string) )
 -> 이 때 Main 씬에서 플레이어 리스트 업데이트에 관련된 OutOfIndex 문제 발생
     Master Player 가 나가서 리스트를 업데이트 해야하는데 타이틀 화면이기 때문에 리스트가 존재하지 않기 때문
 -> Client 는 강제로 나가지는 것이기에 Out Button 의 Destroy(gameobject) 기능을 사용하지 못함
     중복 버그가 발생

==문제==
같은 이름을 가진 방을 만들면 무한 로딩 -> 이미 있는 방이라고 UI 로 알려주어야 함.
닉네임 만들 때 글자수 제한에 대한 기능을 제대로 구현해주는게 좋음 (영어는 괜찮은데 한글은 글자수 버그있음)